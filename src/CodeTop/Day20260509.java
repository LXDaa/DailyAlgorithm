package CodeTop;

/**
 * <a href="https://leetcode.cn/problems/longest-common-prefix/description/">14. 最长公共前缀</a>
 *
 * <h3>🎯 题目描述</h3>
 * <p>编写一个函数来查找字符串数组中的最长公共前缀。</p>
 * <p>如果不存在公共前缀，返回空字符串 ""。</p>
 * <p><b>公共前缀</b>：所有字符串都共同拥有的起始部分。</p>
 *
 * <h3>💡 核心思路：横向扫描法</h3>
 * <ul>
 *   <li><b>基本思想</b>：依次比较相邻两个字符串的公共前缀，逐步缩小范围
 *     <ul>
 *       <li>先用前两个字符串找出公共前缀</li>
 *       <li>再用这个公共前缀与第三个字符串比较，得到新的公共前缀</li>
 *       <li>依此类推，直到处理完所有字符串</li>
 *     </ul>
 *   </li>
 *   <li><b>优化策略</b>：如果在某一步公共前缀变为空串，可以提前终止</li>
 *   <li><b>时间复杂度</b>：O(S)，S 是所有字符串中字符总数的和
 *     <ul>
 *       <li>最坏情况：所有字符串都相同，需要比较所有字符</li>
 *     </ul>
 *   </li>
 *   <li><b>空间复杂度</b>：O(1)，只使用了常数个额外变量</li>
 * </ul>
 *
 * <h3>🔄 算法步骤</h3>
 * <ol>
 *   <li><b>边界处理</b>：如果数组为空或长度为 0，返回空字符串</li>
 *   <li><b>初始化</b>：将第一个字符串作为初始的公共前缀 ans</li>
 *   <li><b>横向扫描</b>：从第二个字符串开始遍历
 *     <ol>
 *       <li>逐字符比较 ans 和当前字符串 strs[i]</li>
 *       <li>找到第一个不匹配的字符位置 j</li>
 *       <li>更新 ans 为 ans.substring(0, j)（截取公共部分）</li>
 *       <li>如果 ans 变为空串，提前返回 ""</li>
 *     </ol>
 *   </li>
 *   <li><b>返回结果</b>：返回最终的公共前缀 ans</li>
 * </ol>
 *
 * <h3>📊 ASCII 演示过程</h3>
 * <pre>
 * 示例1：存在公共前缀 ["flower", "flow", "flight"]
 *
 * 初始化：ans = "flower"
 *
 * 【i=1】比较 ans="flower" 和 strs[1]="flow"
 *
 *   f l o w e r
 *   f l o w
 *   ✓ ✓ ✓ ✓
 *         ↑
 *      j=4 时，strs[1] 结束
 *
 *   公共前缀长度 j = 4
 *   ans = "flower".substring(0, 4) = "flow"
 *
 *   状态：ans = "flow"
 *
 * 【i=2】比较 ans="flow" 和 strs[2]="flight"
 *
 *   f l o w
 *   f l i g h t
 *   ✓ ✓ ✗
 *       ↑
 *    j=2 时，'o' != 'i'
 *
 *   公共前缀长度 j = 2
 *   ans = "flow".substring(0, 2) = "fl"
 *
 *   状态：ans = "fl"
 *
 * 遍历结束，返回 "fl" ✅
 *
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 *
 * 示例2：无公共前缀 ["dog", "racecar", "car"]
 *
 * 初始化：ans = "dog"
 *
 * 【i=1】比较 ans="dog" 和 strs[1]="racecar"
 *
 *   d o g
 *   r a c e c a r
 *   ✗
 *   ↑
 *   j=0 时，'d' != 'r'
 *
 *   公共前缀长度 j = 0
 *   ans = "dog".substring(0, 0) = ""
 *
 *   检测到 ans 为空，提前返回 "" ✅
 *
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 *
 * 示例3：完全相同 ["abc", "abc", "abc"]
 *
 * 初始化：ans = "abc"
 *
 * 【i=1】比较 ans="abc" 和 strs[1]="abc"
 *
 *   a b c
 *   a b c
 *   ✓ ✓ ✓
 *
 *   公共前缀长度 j = 3（完全匹配）
 *   ans = "abc".substring(0, 3) = "abc"
 *
 *   状态：ans = "abc"
 *
 * 【i=2】比较 ans="abc" 和 strs[2]="abc"
 *
 *   a b c
 *   a b c
 *   ✓ ✓ ✓
 *
 *   公共前缀长度 j = 3
 *   ans = "abc".substring(0, 3) = "abc"
 *
 *   状态：ans = "abc"
 *
 * 遍历结束，返回 "abc" ✅
 *
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 *
 * 示例4：包含空字符串 ["ab", "a", "abc"]
 *
 * 初始化：ans = "ab"
 *
 * 【i=1】比较 ans="ab" 和 strs[1]="a"
 *
 *   a b
 *   a
 *   ✓
 *     ↑
 *   j=1 时，strs[1] 结束
 *
 *   公共前缀长度 j = 1
 *   ans = "ab".substring(0, 1) = "a"
 *
 *   状态：ans = "a"
 *
 * 【i=2】比较 ans="a" 和 strs[2]="abc"
 *
 *   a
 *   a b c
 *   ✓
 *
 *   公共前缀长度 j = 1
 *   ans = "a".substring(0, 1) = "a"
 *
 *   状态：ans = "a"
 *
 * 遍历结束，返回 "a" ✅
 * </pre>
 *
 * <h3>🔑 关键技巧解析</h3>
 * <pre>
 * 1️⃣ 为什么叫「横向扫描」？
 *    - 因为是逐个字符串进行比较，像横向扫描一样
 *    - 与之对应的是「纵向扫描」：逐个字符位置比较所有字符串
 *    - 横向扫描更直观，易于理解和实现
 *
 * 2️⃣ 为什么要用 substring 截取？
 *    - 每次比较后，公共前缀只会变短或不变，不会变长
 *    - substring(0, j) 保留前 j 个字符，即公共部分
 *    - 例如：ans="flower", j=4 → ans="flow"
 *
 * 3️⃣ 内层循环的终止条件？
 *    - j < strs[i].length()：防止超出当前字符串的长度
 *    - j < ans.length()：防止超出当前公共前缀的长度
 *    - 两者取较小值，确保不会越界
 *
 * 4️⃣ 为什么可以提前返回？
 *    - 如果某一步 ans 变为空串，说明已经没有公共前缀了
 *    - 后续的字符串再怎么比较，结果也只能是空串
 *    - 提前返回可以避免不必要的比较，提高效率
 *
 * 5️⃣ 时间复杂度分析？
 *    - 设 n 为字符串数组的长度
 *    - 设 m 为最短字符串的长度
 *    - 最坏情况：所有字符串都相同，需要比较 n*m 次
 *    - 平均情况：通常会提前终止，实际比较次数少于 n*m
 *    - 总体：O(S)，S 是所有字符串字符总数
 *
 * 6️⃣ 空间复杂度分析？
 *    - 只使用了 ans 和 j 两个变量
 *    - substring 操作在 Java 中会创建新字符串
 *    - 但这是必要的输出，不计入额外空间
 *    - 额外空间复杂度：O(1)
 *
 * 7️⃣ 与其他方法的对比？
 *    - 纵向扫描：逐个字符位置比较所有字符串
 *    - 分治法：将数组分成两半，分别求公共前缀，再合并
 *    - 二分查找：对最短字符串的长度进行二分查找
 *    - 横向扫描：最简单直观，适合面试
 *
 * 8️⃣ 边界情况处理？
 *    - 空数组：返回 ""
 *    - 单元素数组：返回该元素本身
 *    - 包含空字符串：公共前缀必然是 ""
 *    - 所有字符串相同：返回任意一个字符串
 * </pre>
 *
 * <h3>⚠️ 注意事项</h3>
 * <ul>
 *   <li><b>空指针检查</b>：首先检查 strs 是否为 null 或空数组</li>
 *   <li><b>边界条件</b>：内层循环要同时检查两个字符串的长度</li>
 *   <li><b>提前终止</b>：当 ans 为空时立即返回，避免无效计算</li>
 *   <li><b>substring 使用</b>：substring(0, j) 截取前 j 个字符</li>
 *   <li><b>字符串比较</b>：使用 charAt 逐字符比较，效率高</li>
 *   <li><b>返回值</b>：没有公共前缀时返回空字符串 ""，不是 null</li>
 * </ul>
 *
 * @author lxd
 **/
public class Day20260509 {
    /**
     * 🔍 查找字符串数组中的最长公共前缀
     * <p>使用横向扫描法，逐个字符串比较</p>
     *
     * @param strs 字符串数组
     * @return 最长公共前缀，不存在则返回空字符串
     */
    public String longestCommonPrefix(String[] strs) {
        // 🛑 边界情况：数组为空或长度为 0
        if (strs == null || strs.length == 0) {
            return "";
        }

        // 📝 初始化：将第一个字符串作为初始公共前缀
        String ans = strs[0];

        // 🔄 横向扫描：从第二个字符串开始比较
        for (int i = 1; i < strs.length; i++) {
            int j = 0;

            // 🔍 逐字符比较，找到第一个不匹配的位置
            for (; j < strs[i].length() && j < ans.length(); j++) {
                if (ans.charAt(j) != strs[i].charAt(j)) {
                    break; // ❌ 发现不匹配，退出循环
                }
            }

            // ✂️ 更新公共前缀：截取前 j 个字符
            ans = ans.substring(0, j);

            // ⚡ 优化：如果公共前缀为空，提前返回
            if (ans.equals("")) {
                return ans;
            }
        }

        // ✅ 返回最终的公共前缀
        return ans;
    }
}
