package Hot100.技巧;

/**
 * @author lxd
 **/
public class Hot096 {
    /**
     * <a href="https://leetcode.cn/problems/single-number/description/">136. 只出现一次的数字</a>
     *
     * <h3>🎯 题目描述</h3>
     * <p>给你一个非空整数数组 nums，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。</p>
     * <p>你必须设计并实现线性时间复杂度的算法来解决此问题，且该算法只使用常量额外空间。</p>
     *
     * <h3>💡 核心思路：异或运算</h3>
     * <ul>
     *   <li><b>基本思想</b>：利用异或运算的性质求解
     *     <ul>
     *       <li>异或运算性质1：a ^ a = 0（相同数字异或结果为 0）</li>
     *       <li>异或运算性质2：a ^ 0 = a（任何数字与 0 异或结果为自身）</li>
     *       <li>异或运算性质3：a ^ b ^ c = a ^ c ^ b（交换律）</li>
     *       <li>遍历数组，将所有元素依次异或</li>
     *       <li>出现两次的元素会相互抵消（结果为 0）</li>
     *       <li>最终结果就是只出现一次的元素</li>
     *     </ul>
     *   </li>
     *   <li><b>为什么用异或运算</b>：
     *     <ul>
     *       <li>满足线性时间复杂度 O(n)</li>
     *       <li>满足常量空间复杂度 O(1)</li>
     *       <li>实现简单，代码简洁</li>
     *       <li>不需要额外的数据结构</li>
     *     </ul>
     *   </li>
     *   <li><b>时间复杂度</b>：O(n)，遍历一次数组</li>
     *   <li><b>空间复杂度</b>：O(1)，只使用常量空间</li>
     * </ul>
     *
     * <h3>📊 ASCII 演示过程</h3>
     * <pre>
     * 示例：nums = [2, 2, 1]
     *
     * 初始化：res = 0
     *
     * num = 2：
     *   res = 0 ^ 2 = 2
     *   → res = 2
     *
     * num = 2：
     *   res = 2 ^ 2 = 0
     *   → res = 0
     *
     * num = 1：
     *   res = 0 ^ 1 = 1
     *   → res = 1
     *
     * 结果：res = 1 ✅
     * 出现两次的元素（2）相互抵消，剩余只出现一次的元素（1）
     *
     * 示例2：nums = [4, 1, 2, 1, 2]
     *
     * res = 0
     * res = 0 ^ 4 = 4
     * res = 4 ^ 1 = 5
     * res = 5 ^ 2 = 7
     * res = 7 ^ 1 = 6
     * res = 6 ^ 2 = 4
     *
     * 结果：res = 4 ✅
     * 出现两次的元素（1, 2）相互抵消，剩余只出现一次的元素（4）
     *
     * 示例3：nums = [1]
     *   res = 0 ^ 1 = 1 ✅
     * </pre>
     *
     * <h3>🔑 关键技巧解析</h3>
     * <pre>
     * 1️⃣ 异或运算有哪些性质？
     *    - 性质1：a ^ a = 0（相同数字异或结果为 0）
     *    - 性质2：a ^ 0 = a（任何数字与 0 异或结果为自身）
     *    - 性质3：a ^ b ^ c = a ^ c ^ b（交换律）
     *    - 性质4：a ^ b ^ a = b（结合律）
     *    - 这些性质是解题的核心
     *
     * 2️⃣ 为什么遍历异或能找到只出现一次的元素？
     *    - 出现两次的元素：a ^ a = 0，相互抵消
     *    - 出现一次的元素：0 ^ x = x，保留下来
     *    - 最终结果就是只出现一次的元素
     *    - 例如：[2,2,1] → (2^2)^1 = 0^1 = 1
     *
     * 3️⃣ 为什么初始值是 0？
     *    - 根据性质2：a ^ 0 = a
     *    - 初始化为 0 不会影响第一个元素的异或结果
     *    - 如果初始化为其他值，会破坏异或的性质
     *    - 例如：初始化为 x，最终结果会变成 x ^ target
     *
     * 4️⃣ 如何处理数组长度为 1 的情况？
     *    - 当数组只有一个元素时：res = 0 ^ nums[0] = nums[0]
     *    - 直接返回该元素，符合预期
     *    - 不需要特殊处理
     *
     * 5️⃣ 如果有多个元素只出现一次怎么办？
     *    - 当前方法只适用于恰好有一个元素出现一次的情况
     *    - 如果有多个元素出现一次，需要其他方法（如哈希表）
     *    - 例如：[1,2,3]，当前方法会返回 0（1^2^3=0）
     *
     * 6️⃣ 为什么时间复杂度是 O(n)？
     *    - 只遍历一次数组
     *    - 每次迭代只进行一次异或操作（O(1)）
     *    - 总时间：O(n)
     *
     * 7️⃣ 为什么空间复杂度是 O(1)？
     *    - 只使用一个变量 res
     *    - 不需要额外的数据结构
     *    - 空间复杂度：O(1)
     *
     * 8️⃣ 完整流程总结：
     *
     *    res = 0
     *
     *    for num in nums：
     *      res ^= num
     *
     *    return res
     *
     *    等价于：
     *    res = nums[0] ^ nums[1] ^ ... ^ nums[n-1]
     *    = (a1 ^ a1) ^ (a2 ^ a2) ^ ... ^ target
     *    = 0 ^ 0 ^ ... ^ target
     *    = target
     * </pre>
     *
     * <h3>⚠️ 注意事项</h3>
     * <ul>
     *   <li><b>初始值</b>：res 必须初始化为 0</li>
     *   <li><b>异或顺序</b>：异或运算满足交换律，顺序不影响结果</li>
     *   <li><b>适用条件</b>：恰好有一个元素出现一次，其余出现两次</li>
     *   <li><b>时间复杂度</b>：O(n)，线性时间</li>
     *   <li><b>空间复杂度</b>：O(1)，常量空间</li>
     * </ul>
     *
     * @param nums 非空整数数组，除了某个元素只出现一次外，其余每个元素均出现两次
     * @return 只出现一次的元素
     */
    public int singleNumber(int[] nums) {
        // 📊 初始化结果为 0（根据异或性质：a ^ 0 = a）
        int res = 0;

        // 🔄 遍历数组，依次异或所有元素
        for (int num : nums) {
            // 📊 异或运算：相同数字抵消，不同数字保留
            res ^= num;
        }

        // ✅ 返回只出现一次的元素
        return res;
    }
}
